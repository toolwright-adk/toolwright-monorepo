{
  "title": "Linear Bootstrap MCP Server",
  "description": "MCP server for bootstrapping Linear projects from natural language. Provide your Linear API key to get started.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "LINEAR_API_KEY": {
      "title": "Linear API Key",
      "type": "string",
      "description": "API key for accessing the Linear API. Create one at linear.app/settings/api",
      "editor": "textfield",
      "isSecret": true
    }
  },
  "required": ["LINEAR_API_KEY"]
}
